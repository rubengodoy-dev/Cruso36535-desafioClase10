<div class="container my-4">
  <div class="row">
    <div class="col-md-6 offset-md-3">
      <h1 class="text-center h2">Listado de productos HBS</h1>

      {{!-- <table>
        <thead>
          <tr>
            {{#each array.[0]}}
            <th>{{@key}}</th>
            {{/each}}
          </tr>
        </thead>
        <tbody>
          {{#each array}}
          <tr>
            {{#each this}}
            <td>{{this}}</td>
            {{/each}}
          </tr>
          {{/each}}
        </tbody>
      </table> --}}


  <table class="table table-light table-hover">
                <tr>
                    <th>Nombre</th>
                    <th>Precio</th>
                    <th>Foto</th>
                </tr>
                {{#each array}}
                <tr>
                    <td>{{title}}</td>
                    <td>{{price}}</td>
                    <td><img src="{{thumbnail}}" style="max-height: 30px; vertical-align:middle;"></td>
                </tr>
                {{/each}}
            </table>


    </div>
  </div>
</div>




<div class="row">
  <div class="col-md-6 offset-md-3">
    <a href="/" class="btn btn-secondary" style="float: right;">Agregar nuevo producto</a>
  </div>
</div>